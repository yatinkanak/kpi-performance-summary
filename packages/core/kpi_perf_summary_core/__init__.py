"""Shared domain core for the Public Investor KPI Performance Summary app.

Imported by both the FastAPI adapter (``apps/api``) and the FastMCP adapter
(``apps/mcp``) so that business logic lives in exactly one place.
"""

__version__ = "0.1.0"
